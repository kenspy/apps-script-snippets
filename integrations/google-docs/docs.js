/**
 * Get user's information
 *
 * @return {doc} doc
*/
function doc() {
  return {
    /**
    * Returns a JSON format.
    *
    * @param {String} document_id
    * @return {PDF} create a PDF file
    */
    createPDF: function(folder_id, document_id) {
      
    }
  }
}